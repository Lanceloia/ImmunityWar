using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StepSelect : MonoBehaviour
{

    public GameObject choiceBox;        // 选择框 UI
    // Start is called before the first frame update
    void Start()
    {
        choiceBox.SetActive(false);     // 初始隐藏选择框
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
